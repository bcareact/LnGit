# LnGit
This is demo for learn guthub.
<br>
Auther - BAC React

<br>
copy project in local from  remote:
git clone <-- url of project from gitfub -->

<br>
git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"

<br>
display the status of code:
git status

<br>
to mark files that will be save on GitHub (remote) , you can say this file are (staged , marked to confirm that this file be ready save on remote):
git add "file name"
git add intex.html 

git add . (all files which shown and changed and untrack)

<br>
to saved files to GitHub that is changed and marked in local 
git commit -m "you msg"
git commit -m "added html file and update README.md file"


